name: Publish Article

# Merges a trusted author-tool PR once Site Quality passes, then hands off to
# Site Maintenance (task=blog), which rebuilds, commits and deploys.

on:
  workflow_run:
    workflows: ['Site Quality']
    types: [completed]
    branches:
      - 'author/blog/**'
      - 'author/import/**'
      - 'author/delete/**'
      - 'author/edit/**'

permissions: {}

jobs:
  merge:
    name: Merge trusted author PR
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      pull-requests: write
    outputs:
      merged: ${{ steps.pr.outputs.publish }}
    steps:
      - name: Resolve trusted author PR
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -eu

          pr_number="$(jq -r '.workflow_run.pull_requests[0].number // empty' "$GITHUB_EVENT_PATH")"
          if [ -z "$pr_number" ]; then
            echo "No pull request attached to the completed quality run."
            echo "publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          pr_json="$(gh api "repos/$REPO/pulls/$pr_number")"
          state="$(printf '%s' "$pr_json" | jq -r '.state')"
          draft="$(printf '%s' "$pr_json" | jq -r '.draft')"
          base_ref="$(printf '%s' "$pr_json" | jq -r '.base.ref')"
          head_ref="$(printf '%s' "$pr_json" | jq -r '.head.ref')"
          head_repo="$(printf '%s' "$pr_json" | jq -r '.head.repo.full_name')"

          if [ "$state" != "open" ] || [ "$draft" != "false" ] || [ "$base_ref" != "main" ]; then
            echo "PR #$pr_number is not an open, ready PR to main."
            echo "publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$head_repo" != "$REPO" ]; then
            echo "PR #$pr_number comes from $head_repo, not $REPO."
            echo "publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          case "$head_ref" in
            author/blog/*|author/import/*|author/delete/*|author/edit/*) ;;
            *)
              echo "PR #$pr_number is not an author-tool branch: $head_ref"
              echo "publish=false" >> "$GITHUB_OUTPUT"
              exit 0
              ;;
          esac

          files="$(gh api --paginate "repos/$REPO/pulls/$pr_number/files" --jq '.[].filename')"
          if [ -z "$files" ]; then
            echo "PR #$pr_number has no changed files."
            echo "publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          invalid_files="$(printf '%s\n' "$files" | awk 'NF && $0 !~ /^blog-module\/blog-entries\//')"
          if [ -n "$invalid_files" ]; then
            echo "PR #$pr_number contains files outside blog entries:"
            printf '%s\n' "$invalid_files"
            exit 1
          fi

          echo "number=$pr_number" >> "$GITHUB_OUTPUT"
          echo "publish=true" >> "$GITHUB_OUTPUT"

      - name: Merge author PR
        if: ${{ steps.pr.outputs.publish == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: gh pr merge "${{ steps.pr.outputs.number }}" --repo "$REPO" --merge --delete-branch

  publish:
    name: Build and deploy article
    needs: merge
    if: ${{ needs.merge.outputs.merged == 'true' }}
    permissions:
      contents: write
      pages: write
      id-token: write
    uses: ./.github/workflows/publish-blog.yml
    with:
      task: blog
